services:
  - type: web
    name: acet-study
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python src/server.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
